[package]
name = "test_revenue_evidence_stub"
version = "0.2.0"
description = "TEST-ONLY immutable evidence fixture for the hardened qualified revenue CPI chain"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "lib"]
name = "test_revenue_evidence_stub"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build", "anchor-spl/idl-build", "revenue_qualification/idl-build"]
production = []

[dependencies]
anchor-lang = "=1.1.2"
anchor-spl = { version = "=1.1.2", features = ["token"] }
revenue_qualification = { path = "../../../programs/revenue_qualification", features = ["cpi"] }

[workspace]
